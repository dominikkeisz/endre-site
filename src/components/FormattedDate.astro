---
interface Props {
  date?: Date;
  hide?: boolean;
}

const { date, hide = false } = Astro.props;
---

{!hide && date && (
  <time datetime={date.toISOString()}>
    {date.toLocaleDateString('en-us', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })}
  </time>
)}